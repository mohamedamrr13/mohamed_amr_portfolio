{
    "buildCommand": "flutter build web",
    "outputDirectory": "build",
    "installCommand": "git clone https://github.com/flutter/flutter.git --depth 1 && export PATH=\"$PATH:$(pwd)/flutter/bin\" && flutter pub get"
}