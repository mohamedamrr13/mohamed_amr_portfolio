import 'package:flutter/material.dart';

class AppColors {
  static const primaryColorDark = Color(0xff111418);
  static const buttonColorDark = Color(0xff293038);
  static const white = Color(0xffffffff);
}
